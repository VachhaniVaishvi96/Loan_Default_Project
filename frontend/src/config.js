// Centralized API configuration (No environment variables required!)
// In local dev, leave API_BASE_URL as "" (Vite proxy forwards /api to http://127.0.0.1:8000).
// If frontend & backend are hosted separately in production, paste your backend URL here (e.g. "https://your-backend.onrender.com").
export const API_BASE_URL = "";

